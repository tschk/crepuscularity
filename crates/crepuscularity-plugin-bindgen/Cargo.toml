[package]
name = "crepuscularity-plugin-bindgen"
version = "0.1.5"
edition = "2021"
repository = "https://github.com/tschk/crepuscularity"
license = "ISC"
description = "Generate Crepuscularity ABI bindings for plugin languages (equilibrium-ffi)"
publish = true

[dependencies]
equilibrium-ffi = "0.1.3"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
