[package]
name = "crepus_bench_desktop"
version = "0.1.0"
edition = "2021"
license = "ISC"

[[bin]]
name = "crepus_bench_desktop"
path = "src/main.rs"

[dependencies]
gpui = { package = "gpui-ce", version = "0.2.2", default-features = false, features = ["font-kit"] }
gpui_platform = { package = "gpui_ce_platform", version = "0.1.0" }
crepuscularity-gpui = { path = "../../../../crates/crepuscularity-gpui" }

[workspace]
